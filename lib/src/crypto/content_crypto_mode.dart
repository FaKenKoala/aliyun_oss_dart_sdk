/// Denotes the different cryptographic modes available for securing an OSS
/// object via client-side encryption. Crypto mode can be configured via
/// {@link CryptoConfiguration} when the OSS encryption client is constructed.
enum ContentCryptoMode {
  AES_CTR_MODE,
}
